# Dependencies
node_modules/

# Build output
dist/
dist-e2e/
build/
*.tsbuildinfo

# Playwright E2E artifacts
test-results/
playwright-report/
blob-report/
.playwright/

# Internal planning & research docs (kept local — not published).
#
# The two migration guides are the exception: they are written for merchants,
# they are linked from the README, and the docs MCP syncs them from this repo.
# Everything else under docs/ stays private — check with `git check-ignore -v`
# before adding another exception.
docs/*
!docs/migration_direct_api_to_web_sdk.md
!docs/migration_legacy_sdk_to_web_sdk.md

# Tooling / local state
.atl/

# Env & secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
